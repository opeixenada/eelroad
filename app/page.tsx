import { EelLogo } from '@/components/EelLogo';

export default function HomePage() {
    return (
        <div className="min-h-screen bg-white dark:bg-gray-900 flex items-center justify-center w-screen">
            <EelLogo />
        </div>
    );
}